# -*- coding: utf-8 -*-
"""
__main__.py
--------------------------------------------------------------------
■ なぜ / 目的
  - このパッケージを「モジュール実行」(python -m Master.Sample.Neon_RunnerC) したときの入口。

■ 前提 / 依存
  - 本パッケージは Kivy に依存します。
  - 事前に `pip install kivy` を行ってください。

■ 入出力
  - 入力: なし（アプリ内で設定値を読み込み）
  - 出力: Kivy ウィンドウを生成して画面に表示します。

■ 副作用
  - Kivy のログディレクトリにログファイルが作成されます。

■ 例外
  - Kivy が未インストールの場合は ImportError になります。
"""
from .main import main

if __name__ == "__main__":
    main()
